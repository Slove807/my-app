@AGENTS.md

# 프로젝트 규칙 (CLAUDE.md)

## 1. 프로젝트 개요
규제 변화 모니터링 시스템 — 의료기기 인허가 담당자가 특정 디렉토리에 내부 규정 문서를 첨부하면, AI가 기존 문서와 비교해 파일을 업데이트하고 변경 내용을 요약해 챗봇으로 답해주는 POC입니다.
자세한 내용은 [PRD.md](PRD.md)를 참고하세요.

## 2. 기술 스택
- Next.js (App Router) + React + TypeScript
- Tailwind CSS
- 패키지 매니저: npm
- 배포 예정: Vercel
- 인증: Supabase Auth (이메일/비밀번호). 공개 회원가입 없음 — 계정은 관리자가 직접 생성. 관리자/일반 사용자 역할 분리
- 문서 저장: POC 단계에서는 로컬 디렉토리 사용

## 3. 폴더 구조
- `app/` — 화면(라우트)과 API 라우트 코드
- `public/` — 정적 파일(이미지 등)
- `PRD.md` — 서비스 기획서, 기능 결정의 기준 문서
- `.env` — API 키 등 민감 정보 (절대 커밋 금지, `.gitignore`에 등록됨)

## 4. 코딩 규칙
- 컴포넌트/함수는 TypeScript로 작성하고, `any` 타입은 피한다.
- 커밋 전 `npm run lint`로 오류가 없는지 확인한다.
- 새 기능을 추가하기 전에 PRD.md의 "범위와 비범위"를 먼저 확인한다.

## 5. AI(챗봇)가 지켜야 할 도메인 규칙
- 문서 변경 여부는 **개정번호·개정일자**를 기준으로 판단한다.
- 기존 파일은 삭제하지 않고 버전을 보존한다.
- 새로 첨부된 문서에 실제로 있는 내용만 반영하고, 임의로 해석하거나 추측하지 않는다.
- 변경 내용을 요약할 때는 근거가 된 **문서명·조항**을 함께 표시한다.
- 원문에서 확인되지 않는 내용은 답변에 포함하지 않는다.
- 문서 첨부 시 개인정보 포함 여부를 사용자에게 확인 질문하고, 포함된 경우 경고 문구를 표시한 뒤 진행한다.

## 6. 이번 POC에서 만들지 않는 것
- 법령정보센터·식약처 등 외부 사이트 자동 크롤링/모니터링
- 공개 회원가입 (계정은 관리자가 직접 생성)
- 규정에 대한 법률적 해석·자문 제공
- 이메일/슬랙 등 자동 알림 발송
- 다국어 지원, 모바일 앱

## 7. 보안
- OpenAI 등 외부 API 키는 반드시 `.env`에 저장하고 코드에 직접 넣지 않는다.
- `.env` 파일은 절대 git에 커밋하지 않는다 (`.gitignore`로 이미 제외됨).
- 로그인하지 않은 사용자는 모든 화면·API에 접근할 수 없다 (`proxy.ts`에서 세션을 확인).
- 폴더 경로 설정 변경과 스캔 실행(폴더 전체/카테고리)은 관리자 역할(`app_metadata.role === "admin"`)만 할 수 있다. 문서 열람·챗봇 등 나머지 기능은 로그인한 모든 사용자가 쓸 수 있다.
- 역할 정보는 Supabase Auth 사용자의 `app_metadata`에 저장한다 (`user_metadata`는 사용자 본인이 바꿀 수 있어 권한 값으로 쓰지 않는다).

## 8. 작업 규칙
- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다.
- 기술 스택은 PRD에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.
